# features/editor

The text-based editing experience: rendering the transcription as editable
lines, mapping edits back to the source media, and producing an EDL the
Tauri backend renders. Also owns **Filter Words**, **Double Takes**, and
**Silence Cuts** detection (using the `𝄾` quarter-rest glyph).
