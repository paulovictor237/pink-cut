# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# AI models — downloaded at runtime by the app (or via `bun run models:download`).
# Keep the directory tracked (so its presence is documented) but ignore large binaries.
/models/*.tar.gz
/models/*.bin
/models/parakeet-tdt-0.6b-v3-int8/
/models/parakeet-tdt-0.6b-v2-int8/
/models/silero-vad/
