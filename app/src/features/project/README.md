# features/project

Project-level concerns: the list of recent projects, project metadata
(name, video, transcript state), and the local persistence format. Also
serves as the entry point for the **left sidebar** (navigation/structure,
per `AGENTS.md`).
